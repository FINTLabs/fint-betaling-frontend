apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: afk-no

resources:
  - ../../../base

commonLabels:
  fintlabs.no/org-id: afk.no

patches:
  - patch: |-
      - op: replace
        path: "/metadata/labels/app.kubernetes.io~1instance"
        value: "fint-betaling-frontend_afk-no"
      - op: replace
        path: "/spec/orgId"
        value: "afk.no"
      - op: replace
        path: "/spec/env/0/value"
        value: "/afk-no"
      - op: replace
        path: "/spec/url/basePath"
        value: "/afk-no"
    target:
      kind: Application
      name: fint-betaling-frontend

  - patch: |-
      - op: replace
        path: "/spec/basePath"
        value: "/afk-no"
    target:
      kind: FLAISSso
      name: fint-betaling-auth-forward-sso

