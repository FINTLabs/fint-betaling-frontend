apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ofk-no

resources:
  - ../../../base

commonLabels:
  fintlabs.no/org-id: ofk.no

patches:
  - patch: |-
      - op: replace
        path: "/metadata/labels/app.kubernetes.io~1instance"
        value: "fint-betaling-frontend_ofk-no"
      - op: replace
        path: "/spec/orgId"
        value: "ofk.no"
      - op: replace
        path: "/spec/env/0/value"
        value: "/ofk-no"
      - op: replace
        path: "/spec/url/basePath"
        value: "/ofk-no"
    target:
      kind: Application
      name: fint-betaling-frontend

  - patch: |-
      - op: replace
        path: "/spec/basePath"
        value: "/ofk-no"
    target:
      kind: FLAISSso
      name: fint-betaling-auth-forward-sso

